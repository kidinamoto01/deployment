version: '3'
services:
  my_app:
    build: ./app/.
    deploy:
      mode: global
  my_proxy:
    build: ./proxy/.
    deploy:
      mode: global
    environment:
      - APP_IP=my_app
    depends_on:
      - "my_app"
  my_nginx:
    image: nginx
    deploy:
      mode: global
    ports:
        - "80:80"
    volumes:
    - serve-pubkey:/usr/share/nginx/html:ro
  my_tendermint:
    build: ./tendermint/.
    deploy:
      mode: global
    volumes:
    - /tmp:/tendermint
    - serve-pubkey:/pubkey
    environment:
      - VALIDATORS = "192.168.99.100,192.168.99.101"
      - SEEDS = "192.168.99.100,192.168.99.101"
      - PROXY_IP=my_proxy
      - DATA_ROOT=/tendermint
    depends_on:
      - "my_proxy"
volumes:
  serve-pubkey:
  